Role and Goal:Act as a Senior Full-Stack Developer. We are finalizing the MVP for a Next.js/React web application called "Kinetic Coach". I need you to implement two critical missing backend features: Authentication/Persistence and a Rule-Based Progression Engine.Task 1: Authentication and Persistent Storage
Users must create a secure account to access personalized plans and persisted workout history.Implement a basic authentication flow (email/password or Google OAuth). If using Next.js, implement NextAuth.js or Auth.js.Ensure that when a user completes the onboarding flow, their profile data and generated weekly plan are saved to a persistent database (PostgreSQL preferred, as established in the stack).Ensure that if the page is refreshed, the user remains logged in and their current weekly plan loads immediately on the daily dashboard.Task 2: Deterministic Progression Engine (STRICT RULE: DO NOT USE AI FOR THIS)
The progression logic must live exclusively in the backend as deterministic rule code—NOT in AI prompts. Write a standard TypeScript/JavaScript utility function that executes the following rules when a workout is finished and logged:Rule 1 (Progression): If a user completes all prescribed reps for all sets of an exercise at a given weight for 2 consecutive sessions, increase the prescribed weight for the next session by 2.5 kg for compound lifts or 1.25 kg for isolation exercises.Rule 2 (Regression): If a user fails to complete the minimum target reps on 2 consecutive sessions, maintain the current weight or reduce it by one increment.Execution Constraints:Do NOT build any UI for progress charts, streaks, or consistency tracking. We are strictly focusing on the core MVP flow.The primary goal is to ensure a flawless "happy path" for a 3-minute video walkthrough: User logs in -> views generated plan -> logs a workout -> backend calculates next week's weight.Please write the necessary API routes, database schemas, and utility functions, and confirm when they are integrated into the main application flow.